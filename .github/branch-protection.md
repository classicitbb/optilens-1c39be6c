# Branch protection requirement

To prevent broken pull requests from merging, configure your repository branch protection rule (or ruleset) for the default branch to require the **PR Validation / Lint, Typecheck, Build** status check before merge.

Optional but recommended: also require **PR Validation / Release ledger sync check**.
